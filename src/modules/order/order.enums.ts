export enum OrderStatus {
  active = 'active',
  cancelled = 'cancelled',
  complete = 'complete',
  partial = 'partial',
  declined = 'declined',
  archive = 'archive',
  rejected = 'rejected',
  removed = 'removed',
  paused = 'paused',
  expired = 'expired',
  swapped = 'swapped',
}

export enum CrmOrderStatus {
  authorized = 'authorized',
  captured = 'captured',
}
